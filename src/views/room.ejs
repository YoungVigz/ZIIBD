<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System rezewacji hotelu</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
    
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">System rezewacji hotelu</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" href="/users">Użytkownicy</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="/hotels">Hotele</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="/hotels">Pokoje</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="/reservations">Rezerwacje</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    
    <div class="main-container">
        <div class="text-center">
            <h2>Informacje o pokoju: <%= room[2] %></h2>

            <table class="table table-striped mt-4">
                <tbody>
                    <tr>
                        <th scope="row">ID Pokoju</th>
                        <td><%= room[0] %></td>
                    </tr>
                    <tr>
                        <th scope="row">Typ pokoju</th>
                        <td><%= room[2] %></td>
                    </tr>
                    <tr>
                        <th scope="row">Cena</th>
                        <td><%= room[3] %> PLN</td>
                    </tr>
                    <tr>
                        <th scope="row">Dostępność</th>
                        <td><%= room[4] === 1 ? "Dostępny" : "Niedostępny" %></td>
                    </tr>
                </tbody>
            </table>

            <h3>Edytuj pokój</h3>
            <form action="/rooms/<%= room[0] %>?_method=PUT" method="POST">
                <div class="mb-3">
                    <label for="room_type" class="form-label">Typ pokoju</label>
                    <input type="text" class="form-control" id="room_type" name="room_type" value="<%= room[2] %>" required>
                </div>

                <div class="mb-3">
                    <label for="price" class="form-label">Cena</label>
                    <input type="number" step="0.01" class="form-control" id="price" name="price" value="<%= room[3] %>" required>
                </div>

                <div class="mb-3">
                    <label for="availability" class="form-label">Dostępność</label>
                    <select class="form-control" id="availability" name="availability">
                        <option value="1" <%= room[4] === 1 ? "selected" : "" %>>Dostępny</option>
                        <option value="0" <%= room[4] === 0 ? "selected" : "" %>>Niedostępny</option>
                    </select>
                </div>

                <button type="submit" class="btn btn-primary">Zaktualizuj pokój</button>
                <a href="/hotels/<%= room[1]%>" class="btn btn-secondary">Powrót do hotelu</a>
            </form>
        </div>
    </div>

    <footer class="bg-light text-center py-3">
        <p>&copy; 2024 Gabriel Gałęza</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
